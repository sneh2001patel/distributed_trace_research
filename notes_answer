1) Hyperparameters set:
    Classification Eval:
        - Real-data percentages: 0, 10, 30, 50, 70, 100
        - Synth cap: 500 norm + 500 abnorm per generator
        - Classification epochs: 30
        - real holdout ratio: 20%
        - Validation split: 10% real val for TT, synth/heldout vald setup for SN

    Fixed Hierarchical VAE
        - enc_h_dropout = 1.0
        - TT beta_final = 0.05

2)
train-on-synth-test-on-real:
    flat VAE F1: 0.665
    hierarchical VAE F1: 0.665
Minimum useful real data: 10%
Across both systems, 10% real data is the smallest tested real-data portion that consistently improves performance substantially.

3) What baselines were used:

Empirical sampling baseline
This baseline does not learn a neural model. It generates traces by sampling directly from real-data distributions:

    - sample trace size from real node-count distribution
    - sample service from P(service)
    - sample operation from P(operation | service)
    - sample duration from empirical duration distribution
    - build a simple graph structure using random tree / chain / shallow DAG /           reused pattern


Flat VAE baseline
This is a one-layer/non-hierarchical VAE:

    - encode the whole graph into one latent vector z_G
    - no node-level latent variables
    - decode all node attributes and edges from that single graph latent


4) Yes. Real-data integration substantially improves performance, especially for SocialNetwork and for empirical sampling. A small real fraction appears to anchor synthetic data to the real test distribution.

5) Used a supervised graph Classification for normal-vs-abnormal trace detection

For SN:
service embedding
operation embedding
duration encoding
GCN layers
global mean pooling
MLP classifier

For TT:
service embedding
operation embedding
duration encoding
forward and reverse SAGEConv layers
global mean/max pooling
graph statistics such as node count, edge count, density, degree, roots/leaves, unique services/ops
MLP classifier


The old similarity score does not exist, we updated the
------------------------------------------------
Update the Empirical to Random

Stradifeidy sampling

Search for all relevant mentions

--------------------------------------------------------

Good, I have a comprehensive picture now. Here's the full breakdown of every place that needs changing, not just the evaluation section:
Evaluation Section (biggest rewrite)

This is the main target, as you suspected. Specifically:

    RQ2 entirely — reframe from workload discrimination to anomaly detection (normal vs. abnormal within SN and TT)
    RQ3 entirely — replace fixed-size vs. variable-size with the real data proportion experiment (0→10→30→50→70→100%)
    RQ4 — the KS test table and PCA figures reference "fixed-size" generation specifically, so those captions and the surrounding text need updating
    The summary paragraph after RQ4 — it currently says "variable-size generation leads to more balanced classification behavior than fixed-size generation" which will be stale

Introduction (small fixes)

Two spots:

    Line 110 — "The framework supports fixed-size traces...and variable-size traces" — remove or replace since that distinction is no longer an RQ
    Line 117 — the contributions bullet also mentions "support for both fixed-size and variable-size generation" — needs trimming
    RQ2 and RQ3 definitions (lines 124–125) — need to be completely rewritten to match your new structure

Abstract (small fix)

    Line 82 currently says "discriminative classification, and train-on-synthetic-test-on-real experiments" — this is fine, but you should add a phrase about anomaly detection specifically so it matches the new evaluation framing

Industrial Section (minor alignment)

    Lesson 2 (line 770) — "Allowing synthetic traces to vary in size and structure leads to more balanced downstream performance compared to fixed-size generation" — this lesson is tied to the old RQ3, needs to be replaced with a lesson about how much real data is needed
    Practical guidelines bullet (line 786) — "Prefer variable-size trace generation" — same issue, will be orphaned

Methodology Section (one spot)

    Lines 379–381 — the fixed-size and variable-size generation subsection. Since you're no longer making it an RQ, you don't need to remove it entirely, but the framing "Variable-size generation introduces controlled structural diversity, which is important for training robust downstream models" shouldn't imply it's being evaluated comparatively

So to summarize the priority order: Evaluation > Introduction RQs > Industrial lessons > Abstract > Methodology framing. The evaluation section is about 70% of the work. Want to start drafting the new RQ2 and RQ3 sections?





Possible metrics:
nearest-neighbor distance between synthetic and real traces
duplicate-rate / near-duplicate-rate
coverage of service-operation patterns
distribution of unique trace structures
novelty rate: percentage of generated traces whose edge/service-operation sequence does not exactly match a training trace

Generate the same number of synthetic traces as the training set, e.g., 1,244 SN and 1,244 TT. For each trace, create a trace signature:
Signature = set of node labels + set of directed edge labels
Node label: (service, operation)
Edge label: (parent_service, parent_operation) -> (child_service, child_operation)
Metric

How to compute

Meaning


Exact duplicate rate

% synthetic signatures that exactly match any real training signature

Lower means less copying


Unique synthetic ratio

# unique synthetic signatures / # synthetic traces

Higher means more diversity


Novel structure ratio

% synthetic traces whose signature does not appear in training set

Higher means more novelty


Nearest-neighbor similarity score

For each synthetic trace, find most similar real trace using Jaccard similarity over edge labels

Lower/moderate means less memorization


95th percentile NN similarity

95th percentile of nearest-neighbor similarities

Detects near-copied traces



Pleas read it and let me know what do you think of doing it?

Try stratified sampling to show representativeness of generated data. In RQ1 add also :
span count distribution ,  duration distribution ,  service frequency distribution ,  operation frequency distribution ,  graph depth / edge count distribution
Setup: Use the real test traces and the generated synthetic traces for each system separately: SN and TT. For each system:
Generate the same number of synthetic traces as real traces
SN: 1,244 synthetic traces
TT: 1,244 synthetic traces
Stratify real traces by trace size
Use number of spans per trace as the stratum.
Example bins:
Small traces:   1–5 spans
Medium traces:  6–10 spans
Large traces:   11–20 spans
Very large:     >20 spans
For SN, since traces range 3–30 spans, this works well.
For TT, since traces range 1–23 spans, it also works.
Sample synthetic traces according to the same bin proportions
For example, if 40% of real SN traces are in the 6–10 span bin, then 40% of sampled synthetic SN traces should also come from that bin.
Compare distributions within each stratum
For each bin, compare real vs synthetic on:  Service frequency, Operation frequency, Duration distribution, Edge count, Graph depth, Branching factor
Report distribution similarity
Use Jensen-Shannon Distance for service/operation distributions
Wasserstein Distance or MAE for duration distributions
MAE for graph statistics such as edge count and depth

Metric

1–5

6–10

11–20

>20


Service Dist. (JSD)

0.03

0.04

0.05

0.06


Operation Dist. (JSD)

0.04

0.05

0.06

0.07


Duration (Wass.)

0.12

0.15

0.18

0.21


Edge Count (MAE)

0.8

1.1

1.5

2.0


