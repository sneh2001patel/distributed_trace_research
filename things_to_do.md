ok heres the thing, in the current experiment I use dataset of normal SN and normal TT traces, and then I make an hierical VAE using those normal trace data. and then I used the synthetic dataset to train a Binary Classification. the main thing I got feedback for is that, SN uses about 21 microservices and TT uses about 40+, therefore its easier to train the classification. They said to show what happens when we have anomalies. so heres what I want to do.
1. Change the dataset instead of normal traces, I want to use abnormal ones, I think its based on latency, and I am pretty sure. To do this take a look at the SN_Dataset and the TT_dataset directory, use the preprocess.py script to to get useable dataset, and then in that there should be anomalies and normal trace data.
2. Using the abnormal trace data I want to create 2 Hierical VAE basically what I currently have but instead of using two different datasets (SN and TT), I want to the abnormal and normal data for both SN and TT
3. Then use the encoder-decoder system and then generate synthetic data, which then I can train a classification model to identify between normal and abnormal trace data. 
4. Then see how accurate the classifcation models are with the following ratios: 10:90, 30:70, 50:50, 70:30, 100:0, 0:100
5. Later if I have time use multiple different classifcation models (i.e rainforest) to verify the results. 
